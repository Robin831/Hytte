# Changelog

## [1.0.0] - 2026-03-20

### Added

- **AI-powered workout comparison analysis** - New POST /api/training/compare/analyze endpoint generates natural language insights comparing two workouts using Claude, with structured prompts including lap-by-lap data and response caching. (Hytte-0hf)
- **Quiet hours for notifications** - Configure a time window during which push notifications are suppressed. Includes time pickers for start/end times, automatic browser timezone detection, and backend skip logic that silently drops notifications during quiet hours. (Hytte-0oh)
- **Dashboard quick links/bookmarks widget** - Authenticated users can save, remove, and manage personal bookmark links stored as a user preference. Links persist via the `quick_links` preference key. Includes URL validation (http/https only) to prevent XSS, a 50-link limit, max length validation (200 chars for title, 2048 for URL), mobile-accessible remove buttons, and auth-gated rendering. (Hytte-0qe)
- **Previous analyses list on comparison page** - Users with admin access can now browse, load, and delete previous AI comparison analyses directly from the workout comparison page. Access is enforced on both frontend and backend. (Hytte-0qx)
- **Link shortener** - Create and manage short URLs at /go/{code} with click tracking, custom codes, and a management UI behind authentication. (Hytte-0v5)
- **Profile avatar dropdown menu** - Added a Settings link to the profile dropdown in the top-right corner. The dropdown displays user name/email, a Settings link, and a Sign out button, with click-outside-to-close behavior. Shows initials as fallback when no profile picture is available. (Hytte-2jy)
- **System Services (systemd) infra module** - Monitor systemd service units on the local host with configurable service names and real-time status checks. (Hytte-3kc)
- **Per-user module preferences** - Store key-value configuration per infra module per user, enabling module-specific settings like thresholds and intervals. (Hytte-3kc)
- **Webhooks: prefer pre-formatted summary field from payloads** - When a webhook payload includes a `summary` field, the webhooks page now uses it directly for display instead of parsing event/action/type fields. Forge webhooks also support `detail` (shown as a secondary line in the expanded view) and `url` (shown as a clickable link). Any sender can include a `summary` field to get rich display for free â€” no source-specific parsing required. The Forge badge color is now amber. (Hytte-3ld)
- **Weather refresh button** - Added a refresh button (RefreshCw icon) next to the location selector on the weather page. Clicking it re-fetches the forecast for the current location with a spinning icon while loading. (Hytte-3re)
- **Weather location persists across page visits** - The selected weather location is now remembered when you return to the weather page. For signed-in users it's saved as a server-side preference; for everyone else it's stored in localStorage. (Hytte-45p)
- **Wind direction indicator and feels-like temperature on current conditions card** - Shows a rotated arrow icon next to wind speed indicating the direction the wind is blowing. Calculates and displays a "Feels like XÂ°" value using wind chill (when temp â‰¤ 10Â°C and wind â‰¥ 1.3 m/s) or heat index (when temp â‰¥ 27Â°C and humidity â‰¥ 40%) alongside the actual temperature. (Hytte-4hh)
- **Interactive lactate charts with full accessibility and cleanup** - Recharts-based interactive visualizations: lactate curve with threshold markers, dual-axis heart rate chart, threshold trends over time, fixed-speed lactate tracking, and multi-test comparison overlay. All useEffect hooks have proper cleanup/teardown with AbortController support. Comprehensive ARIA roles, labels, and aria-pressed attributes for screen readers. (Hytte-4rx)
- **Per-workout AI training insights** - Workout detail page now has a "Training Insights" button (admin only) that sends workout data to Claude and displays coaching feedback including effort summary, pacing analysis, HR zone observations, and suggestions. Results are cached in the database. (Hytte-5co)
- **Infrastructure monitoring module system** - Added pluggable module framework with common interface, per-user module configuration, backend API (status, list, toggle, detail endpoints), and dashboard UI with status cards and module toggle controls. (Hytte-63p)
- **Lactate test CRUD API** - Database schema and REST endpoints for recording, viewing, updating, and deleting lactate step tests with per-stage data (speed, lactate, heart rate, RPE). (Hytte-65r)
- **Forge webhook source detection and filtering** - Webhooks with an X-Forge-Event header are now classified as "forge" source instead of generic, enabling dedicated notification filtering. Added "The Forge" source toggle and "PR Ready to Merge" event type to notification filter settings. The "Other webhooks" source description now clarifies it covers requests not identified as GitHub or Forge. (Hytte-6u2)
- **Workout name extraction from FIT file metadata** - When importing a .fit file, the workout title is now extracted from the FIT session's SportProfileName or FileId ProductName fields if available, instead of always auto-generating a "Sport YYYY-MM-DD HH:MM" title. Falls back to the auto-generated title when no name is found in the metadata. (Hytte-74b)
- **Weather auto-refresh every 10 minutes** - The weather forecast now automatically refreshes every 10 minutes to keep data fresh. A subtle "Updated X min ago" timestamp shows below the current conditions. Auto-refresh pauses when the browser tab is hidden and resets on manual refresh or location change. (Hytte-7ap)
- **GitHub Actions Status module** - Monitor workflow run status across configured repositories with encrypted token storage. (Hytte-7et)
- **DNS Monitoring module** - Monitor DNS resolution for configured hostnames with support for A, AAAA, CNAME, MX, TXT, and NS record types. (Hytte-7et)
- **Database Stats module** - View SQLite database size and per-table row counts scoped to the authenticated user. (Hytte-7et)

category: Security
- **SSRF protection for GitHub Actions module** - GitHub API HTTP client now uses safeDialContext to validate resolved IPs at connection time. (Hytte-7et)
- **Input validation for GitHub owner/repo fields** - Owner and repo names are validated against GitHub naming rules to prevent path injection in API URLs. (Hytte-7et)
- **Smoothed curves and average reference lines on workout charts** - Pace and HR charts now apply a 12-sample rolling average to reduce noise (especially helpful for treadmill data) and display a horizontal dashed reference line showing the workout average. (Hytte-881)
- **Auto-tagging on workout import** - Workouts are automatically tagged with detected interval patterns (e.g. "4x6m (r1m)") during .fit file import. Auto-tags are displayed with distinct styling and preserved when editing manual tags. (Hytte-9r1)
- **Admin user management page** - New `/admin` page where admins can view all users and toggle feature access per user with a table of toggle switches. Non-admin users are redirected to the dashboard. (Hytte-9uh)
- **Infrastructure monitoring modules with SSRF protection** - Added three built-in infra modules: Service Health Checks (HTTP endpoint monitoring), SSL Certificate Expiry (TLS cert expiration tracking), and Uptime History (historical uptime statistics). Includes API endpoints for managing monitored targets and viewing check results. URLs and hostnames are validated at both input time and connection time using a safe dialer that blocks private/internal IPs and prevents DNS rebinding. HTTP redirects are not followed to prevent redirect-based SSRF. Port range is validated for both SSL cert monitoring and health check service URLs. (Hytte-9uq)
- **Active devices list in notification settings** - Shows all registered push subscriptions with push service hostname, registration date, and "This device" badge. Each device can be individually removed via a dedicated REST endpoint (DELETE /api/push/subscriptions/{id}), with error feedback and local browser subscription cleanup when removing the current device. (Hytte-a6w)
- **Web Push notification infrastructure** - Added VAPID key generation, push subscription API (subscribe/unsubscribe/list), encrypted push delivery helper (RFC 8291), service worker for receiving push notifications, and PWA manifest. (Hytte-b1o)
- **Feature permission system** - Admins can now control which features each user can access via a new user_features table and API. Features like training, notes, links, infra, lactate, webhooks, and Claude AI are gated per-user, with sensible defaults for new users. Admin users bypass all feature checks. The /api/auth/me endpoint now includes the user's feature map. (Hytte-bac)
- **Personal dashboard page** - Added a protected `/dashboard` route showing the logged-in user's profile picture, name, email, and signup date with a welcome message. Unauthenticated users are redirected to the home page. (Hytte-bxk)
- **Auto-tag workouts based on interval structure** - Adds `GenerateAutoTags` to analyze lap data and detect alternating work/rest patterns and uniform repeats, generating compact tags like `auto:6x6m (r1m)` or `auto:8x400m`. Supports distance-based formatting for running, cycling, and other distance sports. (Hytte-d7q)
- **Free-text location search via Nominatim geocoding** - The weather page now has a debounced search input that resolves any place name to coordinates using the OpenStreetMap Nominatim API. Results appear in a keyboard-navigable dropdown; selecting one fetches the MET Norway forecast for that exact lat/lon. The existing Norwegian city quick-access dropdown is preserved. The backend gains a new public `GET /api/weather/search?q=` endpoint and the forecast endpoint now also accepts `?lat=&lon=` params directly. (Hytte-dje)
- **Chat database schema** - Added chat_conversations and chat_messages tables for the AI chat feature. (Hytte-dtn)
- **Hetzner VPS Stats module** - Monitor Hetzner Cloud server status, resources (CPU, RAM, disk), and datacenter info via the Hetzner Cloud API. API tokens are encrypted at rest using AES-256-GCM. (Hytte-eal)
- **Bandwidth / Transfer Usage module** - Track Hetzner Cloud server bandwidth consumption against included transfer quotas with visual progress bars. (Hytte-eal)
- **Docker Containers module** - Monitor Docker container status across multiple remote Docker hosts with CRUD management for host endpoints. (Hytte-eal)
- **Race predictions and training zones** - Added lactate analysis page with race time predictions (Riegel's formula), Olympiatoppen and Norwegian 5-zone training models, and traffic light classification for lactate stages. Available via the Lactate nav item and the `/api/lactate/tests/{id}/analysis` endpoint. (Hytte-efr)
- **Lactate test list, recording wizard, and detail/edit views** - Added dedicated pages for browsing lactate tests, recording new tests with a step-by-step wizard (protocol setup, stage data entry, review), and viewing/editing test details with inline analysis. Replaced the single analysis-only page with proper routing at /lactate, /lactate/new, and /lactate/:id. (Hytte-enj)
- **Auto-tag badge styling** - Auto-generated workout tags now display with a sparkle icon and tooltip to visually distinguish them from manual tags across all views. (Hytte-erf)
- **Weather forecast widget** - Displays current conditions, 12-hour hourly preview, and 7-day forecast for Norwegian locations using the MET Norway (yr.no) API. Location is configurable via a dropdown and integrates with the user's home_location preference from Settings. (Hytte-evd)
- **Dashboard weather, daylight, and Norwegian fun widgets** - Three new dashboard widgets: WeatherWidget shows current conditions for the user's preferred location (fetched from the yr.no API); DaylightWidget computes and displays sunrise/sunset times and a daylight progress bar from the location's coordinates using the NOAA solar algorithm; NorwegianFunWidget shows a rotating Norwegian word of the day with pronunciation, translation, and cultural description, cycling through 20 entries based on day of year. (Hytte-ezb)
- **Lap selection UI on compare page** - When workouts have different lap counts, a side-by-side lap picker lets you manually choose which laps to pair for comparison. Includes accessible labels, bounds checks, error accumulation, and proper cleanup for in-flight requests. (Hytte-f5y)
- **Markdown notes and code snippets** - Adds a personal knowledge base where authenticated users can create, edit, search, and organize markdown notes. Supports syntax-highlighted code blocks, comma-separated tags, title/content search (LIKE-based), and tag filtering. Notes are persisted per-user in SQLite with full CRUD API. (Hytte-fh2)
- **Notification toggle in Settings** - Added a push notification enable/disable toggle with browser permission flow and status display. Shows whether notifications are active, blocked, or pending permission, and warns when subscriptions have degraded. (Hytte-g7i)
- **Training dashboard with .fit file import** - Upload .fit files from Coros (or any device) via drag-and-drop or file picker. Parses sport type, duration, distance, HR/pace/cadence samples, laps/intervals, and calories. Includes workout list, detail view with HR and pace charts, weekly volume summaries, workout comparison with interval-by-interval delta analysis, progression tracking by tagged workout types, and HR zone distribution. (Hytte-gh3)
- **Claude CLI settings in Settings page** - Users can now configure Claude AI for workout analysis: enable/disable toggle, CLI path, model selection (Sonnet/Haiku/Opus), and a Test Connection button to verify the CLI is reachable. (Hytte-gjx)
- **AI-generated workout titles** - After Claude classifies a workout, it now generates a short descriptive title (e.g. 'Interval Training', 'Easy Run', 'Tempo Run') that replaces the generic device title. Manually edited titles are preserved and won't be overwritten by AI. (Hytte-h7v)
- **Send test notification button in settings** - Users can now send a test push notification from the Notifications section in Settings to verify end-to-end delivery is working. (Hytte-i90)
- **Comparison analysis listing and detail API endpoints** - Added GET /api/training/compare/analyses to list all cached AI comparison analyses, GET /api/training/compare/analyses/{id} to retrieve a specific analysis, and DELETE /api/training/compare/analyses/{id} to remove one. All endpoints are user-scoped and require authentication. (Hytte-j5z)
- **Lactate threshold calculation engine** - Five threshold methods (OBLA, Dmax, ModDmax, Log-log, ExpDmax) with API endpoints for calculating thresholds from saved tests or raw stage data. (Hytte-ji5)
- **AI-powered comparison analysis on workout comparison page** - Added "Analyze with Claude" button and analysis display card showing summary, strengths, weaknesses, and observations when comparing two workouts. Admin-only feature with caching and re-analyze support. (Hytte-jlr)
- **Dashboard foundation with grid layout and Greeting/Time Hero widget** - Replaces the placeholder welcome card with a proper widget-grid dashboard. Introduces a reusable `Widget` shell component and a `GreetingWidget` that shows a time-based greeting, a live clock (updated every second), and the current date. The grid layout is responsive (1â†’2â†’3 columns) and ready for additional widgets. (Hytte-kiu)
- **Frontend feature gating** - Routes, navigation items, and dashboard widgets are now gated based on user feature permissions from `/api/auth/me`. Users only see pages and nav links for features they have access to. Admin-only routes are gated by `is_admin`. Loading states show a spinner instead of blank content. (Hytte-kkz)
- **All Forge event types now filterable in notification settings** - Added 6 missing Forge event types (PR Created, Bead Failed, Daily Cost, Worker Done, Bead Decomposed, Release Published) to the notification filter in Settings. Event types are now served from a single backend source of truth via `/api/settings/event-types`, eliminating frontend/backend duplication. Backend validation includes full test coverage. (Hytte-kth)
- **Recently viewed locations in weather dropdown** - The location picker now shows your recently viewed locations at the top for quick access, with all Norwegian cities listed below. Recent locations (up to 10) are stored in localStorage for unauthenticated users and synced via backend preferences for authenticated users. First-time visitors see Oslo, Bergen, and Trondheim as defaults, with coordinates sourced from the backend API (single source of truth). (Hytte-o5q)
- **Navigation sidebar with Lucide icons** - Collapsible left sidebar with icon+label nav items (Home, Dashboard, Weather, Calendar, Webhooks, Notes, Settings), user profile display, mobile slide-out overlay, and localStorage preference persistence. (Hytte-oki)
- **Auto-trigger Claude AI analysis on workout import** - When uploading .fit files, Claude analysis now runs automatically in the background if the user has Claude enabled and the claude_ai feature flag is active. No need to manually click 'Analyze with Claude' after each import. Rule-based autotagging (auto: tags for detected interval patterns) continues to apply on import as before. (Hytte-oor)
- **Forge webhook notification formatting** - Push notifications from Forge webhooks now display human-readable titles (e.g. "Forge: PR Ready to Merge") and include the message, bead ID, and anvil name instead of falling through to the generic formatter. (Hytte-q3x)
- **Flexible lap selection for workout comparison** - The compare API now accepts optional `laps_a` and `laps_b` query parameters (comma-separated 0-based lap indices) to manually pair laps from mismatched workouts. When omitted, the existing automatic 1:1 matching is used. Response now includes `lap_number_a` and `lap_number_b` fields showing which original laps were paired. (Hytte-qmq)
- **Chat frontend page** - New Chat.tsx page with conversation sidebar, message bubbles with markdown rendering and syntax highlighting, loading states, copy-to-clipboard, and responsive mobile/desktop layout. (Hytte-rah)
- **Login page with Google sign-in** - Dedicated login page at /login with a Google-branded sign-in button. Unauthenticated users are redirected here from protected routes, and authenticated users are redirected to the dashboard. (Hytte-rnp)
- **Google OAuth authentication** - Users can sign in with their Google account. Includes session management with secure HTTP-only cookies, user profile storage in SQLite, and a profile avatar dropdown in the header. (Hytte-rxh)
- **Webhook-to-push notification pipeline** - Incoming webhooks now trigger push notifications to the endpoint owner. GitHub events (release, push, pull_request) produce human-readable titles and bodies; generic JSON payloads use the top-level `event`/`action`/`type` field; other payloads fall back to a summary line. Notifications are dispatched asynchronously after each webhook is stored, so webhook response latency is unaffected. When all subscriptions for a user return a definitive dead-endpoint response (410 Gone or 404 Not Found), the `notifications_degraded` user preference is set, enabling the UI to prompt re-subscription. (Hytte-toa)
- **Settings page with user preferences and account management** - Added a full settings page accessible from the profile dropdown with profile info display, theme preference toggle, home city/region selector for weather widget (Norwegian cities), active session management with sign-out-everywhere, and account deletion with confirmation modal. Preferences are stored in a new user_preferences table. (Hytte-u6c)
- **Dashboard widgets for training, fitness, and activity** - Added dashboard widgets for training summary, lactate fitness trends, and a recent activity feed. Widgets are conditional and only render when the user has data. Backend activity feed endpoint aggregates recent workouts, lactate tests, notes, and links. (Hytte-ubp)
- **Hourly forecast toggle (12h / 24h / 48h)** - The hourly weather scroll now supports 12, 24, and 48-hour views via a toggle button group. Date separators appear automatically when the view crosses midnight, making it easy to see which day each hour belongs to. (Hytte-usl)
- **Chat backend API endpoints** - Implements 6 REST endpoints for AI chat conversations: list, create, get, delete, send message, and rename. Includes conversation context building from message history, automatic title generation after first exchange, and feature gating via the "chat" feature flag. (Hytte-xsg)
- **AI-powered workout classification via Claude** - Admin users can now click "Analyze with Claude" on workout detail pages to get AI-generated workout tags and summaries. Results are cached to avoid re-running, and a "Re-analyze" option is available to force a fresh analysis. AI-generated tags appear with a purple badge throughout the app. (Hytte-yjm)
- **Notification filters** - Per-source (GitHub, generic) and per-event-type (push, pull request, release) toggles in notification settings. Filtered events are silently skipped in the push dispatch pipeline. Filter preferences are pre-fetched to avoid per-dispatch DB queries. (Hytte-yr1)
- **Webhook receiver and inspector** - Create webhook endpoints at `/api/hooks/<id>` to capture incoming HTTP requests. Inspect headers, body, method, query string, and timestamp in a live-updating UI powered by Server-Sent Events. (Hytte-zsf)

### Changed

- **Webhooks: rich payload parsing and formatted display** - The request inspector now parses webhook bodies and shows smart summaries in the list (e.g. "Release published: v0.3.1", "Push to main: 3 commits"). Expanded view shows pretty-printed JSON with syntax highlighting, headers collapsed by default, clickable URLs extracted from body, and copy-curl and copy-JSON buttons. Known sources (GitHub, Stripe, Slack) are detected from headers and shown with a colored badge. (Hytte-cbb)
- **Smarter webhook event summaries with source-aware formatting** - The generic webhook handler now humanizes snake_case event names (e.g. `release_published` â†’ `Release published`), appends version/tag info, extracts project context from URLs, and shows a detail line for description/message fields. Forge webhooks (Go-http-client User-Agent) are now detected as a distinct source with an orange `FG` badge and dedicated parsing for `event`, `version`, `release_url`, and `changelog_summary` fields. (Hytte-e2b)
- **Updated ESLint to v10** - Major version bump from ESLint 9.39.4 to 10.0.3, with @eslint/js updated to 10.0.1. (Hytte-luf)
- vite 7.3.1 â†’ 8.0.0 (now uses rolldown bundler)
- @vitejs/plugin-react 5.1.1 â†’ 6.0.1
- @types/node 25.3.5 â†’ 25.5.0
- react-is added as explicit dep (required by recharts under rolldown's
  stricter module resolution)
- **Relaxed similar workout matching to allow Â±1 lap difference** - FindSimilarWorkouts now matches workouts with the same sport and lap counts within Â±1, comparing overlapping lap durations for similarity. This surfaces more comparison candidates for workouts that are structurally close but not identical. (Hytte-v5b)
- **Moved Hetzner API token management to Settings** - The Hetzner Cloud API token input is now in the Integrations section of Settings, alongside other configuration. The Infra page shows a configured/not-configured status with a link to Settings. (Hytte-yqy)

### Fixed

- **Auto-tag now correctly identifies interval patterns in workouts with warmup/cooldown laps** - Warmup and cooldown laps that deviate more than 50% from both the Q1 and Q3 reference durations are trimmed before pattern detection, fixing false negatives on all imported workouts. (Hytte-0j1)
- **Login button moved to header and dark-themed** - The "Sign in with Google" button now only appears in the top-right header area (matching the profile dropdown position) and uses dark theme styling consistent with the rest of the site. Removed the separate login page and duplicate sign-in button from the home page. (Hytte-1ix)
- **HR chart Y-axis and X-axis display cleanup** - Y-axis now shows rounded integers instead of raw floats, and X-axis no longer shows duplicate minute labels. (Hytte-5w6)
- **Fix Hetzner API token saving failing with 'failed to save token'** - Auto-generate a persistent encryption key when ENCRYPTION_KEY env var is not set, so token storage works out of the box. Key file is stored in the user config directory with no working-directory fallback. Corrupt key files (wrong length, invalid hex) are detected and regenerated automatically. File permissions are verified on read and tightened to 0600 if too open. (Hytte-7o2)
- **AI-generated workout titles now apply correctly** - The "Analyze with Claude" feature was not updating workout titles because the condition was too restrictive: it required both title and title_source to be empty, but .fit imports set title_source to 'device'. Relaxed the condition to allow AI to overwrite any title except user-set ones. (Hytte-8u5)
- **Show pending analysis status during background Claude analysis** - After importing a workout, the detail page now shows an "Analysis in progress..." spinner when background analysis is running, preventing users from triggering redundant manual analysis. The button is disabled during pending state and shows a retry option on failure. (Hytte-9ik)
- **HR Zone Distribution now shows actual BPM ranges** - Each zone row displays the BPM boundaries (e.g., "135â€“148 bpm") and time-in-zone alongside the existing percentage bar. (Hytte-arh)
- **Reimporting workouts after delete no longer fails with UNIQUE constraint** - SQLite `PRAGMA foreign_keys=ON` is a connection-level setting; the database connection pool could open new connections that lacked the pragma, causing `ON DELETE CASCADE` to silently not fire. Fixed by capping the pool to one open connection so the pragma is always in effect. (Hytte-c93)
- **Auto-cleanup orphaned workout child rows on startup** - Databases with orphaned rows in workout_laps, workout_samples, or workout_tags (left behind by deletes before the cascade fix) are now automatically cleaned up when the app starts, unblocking .fit file reimports. (Hytte-ccf)
- **Admin user can now see all feature-gated pages** - The /api/auth/me response returned features as a sibling of the user object, but the frontend only stored the user object, losing the features map. Now merges features into the user state so hasFeature() works correctly for all users. (Hytte-d74)
- **Training zone HR calculations now use max heart rate** - Added a "Max HR" setting to the Settings page. Training zones now scale properly using max HR as the zone 5 ceiling, with the threshold HR sitting at the zone 4/5 boundary. When max HR is not configured, a warning is shown on the training zones section instead of silently computing compressed zones. (Hytte-tsi)
- **Weather search now shows specific place names instead of duplicate municipality names** - The geocoding search dropdown now displays the most specific available name (hamlet, suburb, village, etc.) as the primary label, with municipality and county as context (e.g. "HaugsvÃ¦r, AskÃ¸y, Vestland"). Previously all results from the same area showed only the municipality/county name, making them indistinguishable. (Hytte-xvs)

### Security

- **Claude AI features restricted to admin users** - Added is_admin flag to user records (first registered user is auto-admin). Claude-related preferences are filtered from GET and rejected on PUT for non-admin users. The Claude AI section in Settings and the claude-test endpoint are hidden/blocked for non-admin users, and the frontend conditionally renders Claude UI elements based on admin status. (Hytte-2lp)
- **Route authentication enforcement** - All routes except the landing page (/) and the login page (/login) now require authentication. Unauthenticated users are redirected to the landing page. Backend API routes return 401 for unauthenticated requests (except /api/health and /api/auth/*). (Hytte-8me)
- **Session management with secure cookies** - Server-side sessions stored in SQLite with HttpOnly/Secure/SameSite cookie flags. Includes RequireAuth/OptionalAuth middleware, /api/auth/me and /api/auth/logout endpoints, and automatic expired session cleanup. (Hytte-yte)

