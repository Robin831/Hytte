category: Added
- **Forge Settings page** - Admin settings page at /forge/settings to view and edit the `~/.forge/config.yaml` configuration from the browser. Supports editing global settings, per-anvil config, and notification settings. Saving triggers a config write and Forge hot-reload. (Hytte-fcn0)
