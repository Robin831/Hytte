category: Fixed
- **Dashboard clock now shows 24-hour format** - The `formatTime` utility now defaults to `hour12: false`, so all time displays across the app (Dashboard, Training, Weather) consistently use 24-hour format. (Hytte-wapq)
