category: Added
- **Streak warning and weekly summary schedulers** - Extracted `CheckStreakWarnings` and `SendWeeklySummaries` into `internal/stars/scheduler.go` with a `SchedulerDB` interface for testability. Streak warnings fire at 7 PM in the user's timezone; weekly family summaries fire on Monday at 8 AM in the parent's timezone. Both functions are wired into the background daemon loop. (Hytte-9945)
