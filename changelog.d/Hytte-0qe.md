category: Added
- **Quick Links dashboard widget** - A new bookmarks widget on the dashboard that lets users save and open frequently visited URLs. Links are stored per-user in the `quick_links` preference key (JSON-encoded) and synced to the backend, so they persist across devices and sessions. (Hytte-0qe)
