category: Fixed
- **Predictor honesty round 2 and the coach's silent memory loss** - The interval-to-threshold pace adjustment is now gated on the HR the work actually cost (reps at/above threshold HR keep the +6 s/km continuous penalty, reps just under it are threshold pace as-is, clearly sub-threshold reps correct the other way), the durability check counts treadmill runs (belt pace is fake, time on feet is not), indoor quality sessions (HR and durations only) plus the athlete-measured treadmill calibration now reach the AI's facts, and velocity-based VO2max estimates are no longer computed for — or read from — indoor workouts. Separately, the Stride plan chat no longer loses its memory silently: any turn starting a fresh Claude session (auto reset, manual reset, or a failed resume) replays the recent stored conversation into the prompt and explicitly tells the coach how many older messages it cannot see, a failed resume now emits a visible context-reset marker and resets the accounting floor, the auto-reset trigger measures the real native session file (which carries tool calls and reasoning the stored-bytes estimate missed by ~13×), and the stream timeout rose to 420s as a stopgap. (Hytte-htnjp)
