category: Fixed
- **Stride coach now sees workout laps and respects treadmill speed reports** - The eval prompt was using only the workout summary row (avg HR, avg pace, distance, duration), so short-rep interval sessions read as easy aerobic runs because the recovery jogs dragged the averages down. The prompt now includes the per-lap table from `workout_laps` whenever ≥3 laps exist, splits the runner's post-workout self-report out from generic notes with a stronger "prefer this over aggregate inference" framing, and adds an indoor/treadmill callout telling the coach that the watch's pace is unreliable but chest-strap HR is fine. (Hytte-tfub)
