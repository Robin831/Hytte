category: Added
- **Workout-to-plan matching** - Added `internal/stride/match.go` with `MatchWorkoutToSession` and `MatchAllWorkouts` functions that pair completed workouts with planned Stride sessions using exact date matching and ±1-day fuzzy matching by activity type. (Hytte-xlzp)
