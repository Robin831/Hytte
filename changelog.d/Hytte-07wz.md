category: Added
- **Weekly training load chart** - Added a stacked bar chart to the Training Trends page showing easy load (green) and hard load (orange) per week, with a chronic load line overlay. Extracted as a `WeeklyLoadChart` component and backed by the `GET /api/training/load` endpoint. (Hytte-07wz)
