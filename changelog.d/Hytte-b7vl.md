category: Changed
- **Pokémon scanner is now fire-and-forget** - The camera no longer waits for Claude to match a card. Each capture posts to the async queue, an inline "Sent ✓ — view in Scanned" toast confirms, and the scanner resets immediately so the kid can scan the next card. The result-modal / matched-candidates UI moved to the Scanned page. 429 responses now render a "Daily scan limit reached" message and halt auto-capture. The Pokémon sidebar entry shows a small red badge with the count of matched/no_match/failed jobs that need review (capped at "9+"), backed by a new `GET /api/pokemon/scans/counts` endpoint polled every 30 s. (Hytte-b7vl)
