category: Changed
- **Training list decluttered and stride evaluations go live** - The workout list no longer renders tag chips (the unbounded per-workout structure tags had pushed the actual workouts below the fold; tag data is kept and the free-form AI structure tag is no longer minted), stride evaluations now appear the moment the coach finishes via server-sent events — with an "evaluating" indicator while the background call runs — instead of requiring a page refresh, the upload dropzone is a slim row instead of a full-width panel, Backfill moved out of the primary header, and the Training header gained a Stride link. (Hytte-c76mn)
