category: Changed
- **Pokémon scanner crops to the detected card, requests 1080p, and shows a confirm/retake preview** - The auto-capture path now crops the captured frame to the locked card rectangle (with 5% padding and frame clamping) so Claude vision receives a tighter, higher-resolution image of just the card instead of the kid's hand and the table. The camera is requested at 1920×1080 (browser falls back if unavailable) so the cropped region has enough pixels for the set symbol and collector number. A brief preview overlay shows the captured image with Send/Retake buttons and auto-proceeds after 1.5 s, preserving the fire-and-forget flow while giving a quick out on a bad capture. (Hytte-5igs)
