category: Added
- **Regnemester confetti + milestone effects** - Blitz rewards every 5-answer streak with a confetti burst and a scale-pop on the streak badge, and scales end-of-run confetti by score, promoting to a full-screen rainbow burst when the run takes #1 on the all-time leaderboard. Marathon flashes the play surface at 3:00, 4:00 and 5:00 elapsed, and on a new PB finish fires a standard burst, upgraded to a golden burst under 5:00 and a rainbow burst + screen shake under 3:00. All effects honour `prefers-reduced-motion`. The Finish API now also returns the caller's all-time `leaderboard_rank` so the client knows when to celebrate #1. (Hytte-l615)
