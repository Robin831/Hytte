category: Fixed
- **Allowance tab titles no longer show raw i18n keys** - Removed trailing commas from `myChores.extras` object in all three locale files (en, nb, th), which caused invalid JSON and prevented the entire `allowance` namespace from loading. (Hytte-g2ht)
