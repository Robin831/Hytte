category: Added
- **Variable expense auto-update after credit card import** - After a confirmed CSV import, the monthly total of all non-payment transactions is calculated and automatically written to the linked variable bill entry. A variable bill can now be linked to a credit card via a new `credit_card_id` field. (Hytte-db4p)
