category: Added
- **Stride workout library with AI designer** - The Stride page gains a workout library: reusable session definitions with ratings, usage counts, and training-block membership (base/build/peak/taper, multiple per workout), designed conversationally in an AI chat instead of punched in manually. The weekly plan generator now receives the library with rotation rules — the pinned reference session (seeded as 6×6min @ threshold, the half-marathon benchmark) appears in every week unchanged, while other quality sessions must rotate and avoid anything used in the last three weeks — ending the same-intervals-every-week loop. Plan sessions taken from the library carry its id, so usage stats are exact. (Hytte-pysls)
