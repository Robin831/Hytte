category: Fixed
- **Backdated imports land in the right place when refreshing the training list** - Clicking "New workouts available" now folds the fetched page into the list in `started_at` order instead of prepending it, so a backdated .fit import (or an edited start time) appears in its chronological slot, edits replace the existing row, and workouts deleted elsewhere disappear. A refresh that comes back with a whole page of workouts newer than everything on screen (a bulk import) now replaces the list instead of leaving the workouts in between unreachable by "Load more". (Hytte-7cpcw)
