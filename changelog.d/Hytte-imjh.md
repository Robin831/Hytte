category: Added
- **Tabs component** - Added reusable `Tabs`, `TabList`, `TabTrigger`, and `TabPanel` components in `web/src/components/ui/tabs.tsx` with `segment` and `pills` visual variants. Refactored `AllowancePage` (5 tabs) and `LactateInsights` (3 tabs) to use the new component for consistent tab UI across the app. (Hytte-imjh)
