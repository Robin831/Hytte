category: Fixed
- **Saved news articles now react to votes, reads and relevance scores** - On the News page's Saved tab, thumbs up/down highlighted nothing and opening an article never dimmed it, because the card actions only patched the feed's list. Saved cards now update their own state (and stay in sync with the Feed tab), and they show the match score badge when AI ranking is on instead of hiding it unconditionally. The vote toggle rules are shared between both tabs via a new `votePatch` helper. (Hytte-9ejak)
