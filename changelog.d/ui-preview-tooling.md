category: Added
- **ui-preview screenshot tooling** - `tools/ui-preview/` runs a throwaway Hytte instance against a copy of the production database with an injected session, then screenshots or measures any page headlessly at desktop and mobile widths (Playwright/Chromium). Used for layout reviews and visual verification; includes a project skill so agent sessions discover it.
