category: Added
- **Stride: talk to the coach about workout evaluations** - Each workout evaluation now carries a comment thread: reply to the coach's assessment and get a short conversational answer in seconds, without triggering a full re-evaluation. The first evaluation can now include up to 2 clarifying questions (only when the answer would change the assessment) which seed the thread; and when a correction genuinely changes the verdict, the coach revises the stored evaluation in place — same row, marked "Evaluation updated" — so next week's plan generation sees the corrected picture. Threads are keyed by workout (or plan+date) so they survive re-evaluations. Also fixes the manual day re-run silently dropping the runner's post-workout self-report from the prompt. (Hytte-sevc)
