category: Added
- **Salary: config and commission calculation engine** - Adds the `internal/salary` package with database schema (`salary_config`, `salary_commission_tiers`, `salary_tax_brackets`, `salary_records`), a progressive commission calculator with four default Norwegian tiers (0–60 k at 0%, 60–80 k at 20%, 80–100 k at 40%, 100 k+ at 50%), a progressive tax bracket engine, `EstimateMonth` for prorated base salary and net pay projection, `AbsenceDayCost` for absence day costing, and `GetHoursWorked` integration with the Work Hours module. Gated behind a new `salary` feature flag (default off). (Hytte-6y0o)
