category: Fixed
- **Fix queue labels shown as raw JSON** - Labels returned as JSON array strings are now properly parsed instead of comma-split, preventing brackets from appearing in the UI and fixing duplicate forgeReady button detection. (Hytte-mmvq)
- **Add tooltips to queue label action buttons** - The tag icon, remove, and add buttons in FullQueueCard now show descriptive tooltips on hover. (Hytte-mmvq)
