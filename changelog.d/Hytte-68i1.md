category: Added
- **Confetti animation on level-up** - Stars page now detects when the user has levelled up (comparing current level to the last-seen level stored in localStorage) and fires a ~50-particle confetti burst animation in gold, orange, purple, and blue. Confetti falls from the top of the screen over ~3 seconds then cleans itself up automatically. (Hytte-68i1)
