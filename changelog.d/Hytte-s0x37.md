category: Changed
- **gofmt formatting in weather and API router** - Reformatted `internal/weather/handler.go` and `internal/api/router.go` with gofmt: aligned the `NorwegianLocations` map, const block and `nominatimResult`/`nominatimAddress` struct tags, sorted the router import block, and removed a trailing-whitespace line. Formatting only — no behavior change. (Hytte-s0x37)
