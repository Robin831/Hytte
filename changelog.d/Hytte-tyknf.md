category: Fixed
- **Race-prediction envelope is now asymmetric** - The AI's per-distance clamp around the deterministic baseline was a symmetric ±10%, which on a ~1:47 half baseline legally permitted 1:36 — a number nothing in the data supports; every predictor failure observed so far has been optimism, never pessimism. The clamp is now at most 3% faster than baseline but up to 10% slower, a regression test built from the athlete's real 4 August outdoor lap rows pins the half baseline to the coach-reviewed band and makes sub-1:40 unreachable by construction, and the `react-hooks/set-state-in-effect` lint error in the workout library's initial fetch is fixed. (Hytte-tyknf)
