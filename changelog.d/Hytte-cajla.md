category: Added
- **Card search inside a Pokémon set** - The set view now has a search box that filters the grid by card name or collector number (`6`, `006` and `006/165` all find #6). The query is mirrored to `?q=` so reloads, Back and shared links keep it, and it composes with the all/owned/missing and variant chips with a hit count and a no-matches message. (Hytte-cajla)
