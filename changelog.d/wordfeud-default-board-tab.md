category: Changed
- **Wordfeud opens on the Board tab** - The board (live games + solver) is now the default tab instead of the word finder, so the daily-driver view loads without an extra click. The finder remains one tab away and direct links with ?tab=finder still work.
