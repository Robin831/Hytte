category: Fixed
- **Frontend test suite green and wired into CI** - Fixed all 37 long-failing frontend tests plus 3 more that had never been reached: scanner tests now hand the component a real MediaStream instance (happy-dom type-checks srcObject), the SoundEngine Audio stub is constructible under vitest 4, lazy-loaded TodayView widgets are awaited, and stale assertions were updated to current behavior (FamilyChat client_id payload, Pokémon '—' set value and data-ownership tiles, split pause min/sec inputs, mounted-but-hidden tab panels). Root-caused the worker crashes and ~10-minute wall time to an infinite render/fetch loop: Suggestions.test.tsx's react-i18next mock returned a new t function every render, re-triggering NewSuggestionForm's [open, t] effect until the fork OOM'd — t is now cached per namespace and the suite finishes in under a minute. `npm test` is now a CI step, and CI also runs on pushes to main (not just PRs). (Hytte-fets)
