category: Added
- **Lap selection UI on compare page** - When workouts have different lap counts, a side-by-side lap picker lets you manually choose which laps to pair for comparison. Includes accessible labels, bounds checks, and error accumulation so multiple failures are reported together. (Hytte-f5y)
