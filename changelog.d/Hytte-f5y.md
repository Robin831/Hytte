category: Added
- **Lap selection UI on compare page** - When workouts have different lap counts, a side-by-side lap picker lets you manually choose which laps to pair for comparison. Includes accessible labels, bounds checks, and proper error handling for workout fetch failures. (Hytte-f5y)
