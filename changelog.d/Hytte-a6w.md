category: Added
- **Active devices list in notification settings** - Shows all registered push subscriptions with push service hostname, registration date, and "This device" badge. Each device can be individually removed, which also cleans up the local browser subscription if removing the current device. (Hytte-a6w)
