category: Fixed
- **Remove duplicate Recent Events section from Forge Dashboard** - The dashboard showed both a standalone "Recent Events" card and an "Event Log" inside the Live Activity panel, both displaying the same event data. Removed the redundant Recent Events card; the Event Log in Live Activity is retained as it includes filtering and live streaming. (Hytte-2fis)
