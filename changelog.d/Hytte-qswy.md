category: Added
- **Generate plan for current week** - The Stride plan generation now supports a `?week=current` query parameter to generate a plan for the current week instead of next week. The frontend automatically uses current week when no plan exists yet, and next week otherwise. (Hytte-qswy)
