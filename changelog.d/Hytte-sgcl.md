category: Changed
- **Nightly suggestion cleanup** - Before each suggestion run (scheduled and manual), a cleanup pass now prunes stale rows so page-cap slots free up for fresh ideas: AI-generated pending suggestions older than 30 days are deleted, rejected suggestions are purged 30 days after rejection, duplicate pending suggestions (same page + title, case-insensitive) collapse to the newest, and pending suggestions matching an already planned/bead-linked title are dropped as superseded. Plan generation was extracted into an exported PlanSuggestion helper shared by the API handler and maintenance tooling. (Hytte-sgcl)
