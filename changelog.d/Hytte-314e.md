category: Added
- **Streaming chat responses** - The Chat page now streams Claude's reply token-by-token via Server-Sent Events instead of waiting for the full response. A Stop button cancels the stream mid-flight and the partial text stays on screen. (Hytte-314e)
