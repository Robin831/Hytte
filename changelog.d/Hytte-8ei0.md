category: Added
- **Pokémon card scanner i18n and test coverage** - The scanner UI strings are fully translated to English, Norwegian, and Thai, and `CardScanner.test.tsx` exercises the auto-detect → POST → result flow end-to-end: lock-and-capture POSTs exactly once per lock, the result modal renders correctly for matched-single, matched-multi, and no-match payloads, the 30s scan timeout surfaces a toast and resumes scanning, the post-add 2s cooldown suppresses auto rescans, manual shutter bypasses the lock, and the denied/unsupported permission fallbacks are covered. (Hytte-8ei0)
