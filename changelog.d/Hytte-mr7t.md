category: Added
- **Budget limits and category progress bars** - Added `budget_limits` table, `GET /api/budget/limits` endpoint for reading monthly spending limits, and `PUT /api/budget/limits` endpoint for setting them. Monthly summary now includes budget vs actual per category. Frontend shows progress bars (green under 80%, yellow 80-100%, red over budget) with inline limit editing via a pencil icon. Added income vs expenses visual bar at the top of the budget page and "igjen" (remaining) display per category. (Hytte-mr7t)
